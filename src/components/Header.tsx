import React from "react";

const Header: React.FC = () => (
    <header>
        <h1>Meu Blog</h1>
        <nav>
            <a href="#">Home</a> | <a href="#">Sobre</a>
        </nav>
    </header>
);

export default Header;
